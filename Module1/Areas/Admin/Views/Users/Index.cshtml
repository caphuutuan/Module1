@model IEnumerable<Module1.Models.User>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<section class="content py-2">
    <div class="container-fluid">
        <!--Content-->
        <div class="card">
            <div class="card-header">
                <div class="row">
                    <div class="col-8">
                        <h5 class="text-csm">@ViewBag.Title</h5>
                    </div>
                    <div class="col-4">
                        <div class="float-right pt-1">
                            <button type="button" class="btn btn-primary btn-sm">
                                <a href="~/Admin/Users/Create" class="text-white"><i class="fas fa-plus fa-xs"></i>&nbsp; Thêm mới</a>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
                <div class="row">

                    <table id="cate1" class="table table-bordered table-hover">
                        <thead>
                            <tr>
                                <th class="text-center no-sort" style="width:3rem;">ID</th>
                                <th class="no-sort">Tên người dùng</th>
                                <th class="no-sort">Ngày sinh</th>
                                <th class="no-sort">Số điện thoại</th>
                                <th class="no-sort">Địa chỉ</th>
                                <th class="no-sort">Email</th>
                                <th class="no-sort">Role</th>
                                <th class="no-sort text-center" style="width:10rem">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="text-center">@Html.DisplayFor(modelItem => item.UserId)</td>
                                    <td>
                                        <a href="~/Admin/Users/Details/@Html.DisplayFor(modelItem => item.UserId)">
                                            @Html.DisplayFor(modelItem => item.Ten)
                                        </a>
                                    </td>
                                    <td>  @Html.DisplayFor(modelItem => item.NgaySinh)</td>
                                    <td>  @Html.DisplayFor(modelItem => item.Sdt)</td>
                                    <td>  @Html.DisplayFor(modelItem => item.DiaChi)</td>
                                    <td>  @Html.DisplayFor(modelItem => item.Email)</td>
                                    <td>  @Html.DisplayFor(modelItem => item.RoleId)</td>
                                    <td class="text-center">
                                        <label class="switch">
                                            @if (item.Status == 0)
                                            {
                                                <input class="changeStatus" data-id="@item.RoleId" data-controller="Users" type="checkbox" checked>
                                            }
                                            else
                                            {
                                                <input class="changeStatus" data-id="@item.RoleId" data-controller="Users" type="checkbox">
                                            }
                                            <span class="slider round"></span>
                                        </label>
                                        <button type="button" class="btn btn-primary btn-sm" data-toggle="tooltip" data-placement="top" title="Chỉnh sửa">
                                            <a href="@Url.Action("Edit", "Users", new { id = item.RoleId })" class="text-white"><i class="fas fa-pen"></i></a>
                                        </button>
                                        <button type="button" class="btn btn-danger btn-sm" data-toggle="tooltip" data-placement="top" title="Xóa">
                                            <a href="@Url.Action("Delete", "Users", new { id = item.RoleId })" class="text-white"><i class="far fa-trash-alt"></i></a>
                                        </button>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>
